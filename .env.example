# PostgreSQL connection string. Never commit real credentials.
DATABASE_URL="postgresql://user:password@localhost:5432/tijra"

# Add auth and AI provider keys only when those integrations are implemented.
